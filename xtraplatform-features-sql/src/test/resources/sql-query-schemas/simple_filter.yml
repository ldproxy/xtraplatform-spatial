---
- name: externalprovider
  pathSegment: "externalprovider{filter=type=1}"
  filter:
    args:
    - property: type
    - 1
    op: =
  columns:
  - name: id
    pathSegment: id
    type: STRING
    role: ID
